// Stub para web: react-native-webview no está disponible en browser.
// En web los documentos se abren con Linking.openURL desde
// ExpedienteDetailScreen, por lo que esta pantalla nunca se navega.
export default function DocumentViewerScreen(): null {
  return null;
}
